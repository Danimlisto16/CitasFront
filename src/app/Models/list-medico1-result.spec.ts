import { ListMedico1Result } from './list-medico1-result';

describe('ListMedico1Result', () => {
  it('should create an instance', () => {
    expect(new ListMedico1Result()).toBeTruthy();
  });
});
